export const metadata = {
  title: 'Skill Tree',
  description:
    'The interactive calisthenics skill tree — unlock bodyweight skills by completing their prerequisites and track your progress.',
  alternates: { canonical: '/tree' },
};

export default function TreeLayout({ children }) {
  return children;
}
