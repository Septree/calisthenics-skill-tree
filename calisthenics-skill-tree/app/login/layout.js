export const metadata = {
  title: 'Log in',
  description: 'Log in to track your calisthenics progress.',
  alternates: { canonical: '/login' },
};

export default function LoginLayout({ children }) {
  return children;
}
