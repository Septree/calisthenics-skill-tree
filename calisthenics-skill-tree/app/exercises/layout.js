export const metadata = {
  title: 'All Exercises',
  description:
    'Browse every calisthenics skill — push, pull, legs, core and more — with difficulty, prerequisites and video tutorials.',
  alternates: { canonical: '/exercises' },
};

export default function ExercisesLayout({ children }) {
  return children;
}
